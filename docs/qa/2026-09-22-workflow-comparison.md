# Сравнение рабочего процесса — 22.09.2026

Изменена только секция «Меньше рутины» в независимой копии 11housemarketing.
Референс: https://elevenhouse-design-preview.lysovmental.chatgpt.site/#comparison
Принцип композиции: четыре разрозненные задачи слева, единый кабинет справа, связующая графика и финальный CTA. Использованы собственные логотипы, Vetrino/Manrope, navy/gold и существующий космический фон.

Анимация: однократное появление через IntersectionObserver, последовательное прорисовывание связей; выбор пары мышью, касанием и клавиатурой. Нет постоянных таймеров или RAF. Observer и media listener очищаются при размонтировании. Reduced motion оставляет полностью читаемый статичный блок.

Cascade review: изменения CSS ограничены .eh-journey .eh-workflow-shift. Единственное правило предка делает коммерческую обёртку прозрачной при наличии нового блока; тарифы и FAQ сохраняют собственные фоны. Hero, global CSS и зависимости не менялись.

Проверки:
- Production build проходит (существующее предупреждение о размере JS chunk).
- 365 тестов проходят. Существующий тест RU в header падает; experiment physicalDial не запускается из-за отсутствующего модуля. Новые проверки сравнения проходят.
- Браузер: desktop, 390 и 320 px, горизонтального overflow нет.
- Клавиатурный Tab переключает связанную пару; четыре результата остаются читаемыми.
- Reduced motion: data-animate=false, animation-name=none; ошибок console нет.
- Сверка SHA256 с состоянием до задачи: изменены только OneClientStory.tsx, workflowComparison.css и связанные проверки landingCopy.test.ts; добавлен OneClientStory.test.tsx и этот отчёт.
